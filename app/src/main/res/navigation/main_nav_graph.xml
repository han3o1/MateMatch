<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/feedFragment">

    <!-- 🏠 Feed -->
    <fragment
        android:id="@+id/feedFragment"
        android:name="com.mp.matematch.main.ui.feed.FeedFragment"
        android:label="Feed"
        tools:layout="@layout/fragment_feed" />

    <!-- 💗 Liked -->
    <fragment
        android:id="@+id/likedFragment"
        android:name="com.mp.matematch.main.ui.liked.LikedFragment"
        android:label="Liked"
        tools:layout="@layout/fragment_liked" />

    <!-- 💬 Chat -->
    <fragment
        android:id="@+id/chatFragment"
        android:name="com.mp.matematch.main.ui.chat.ChatFragment"
        android:label="Chat"
        tools:layout="@layout/fragment_chat" />

    <!-- 👤 Profile -->
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.mp.matematch.main.ui.profile.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile" />

</navigation>
