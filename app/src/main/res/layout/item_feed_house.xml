<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="6dp">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- 집 사진 -->
        <ImageView
            android:id="@+id/imageHouse"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:src="@drawable/sample_house"
            android:contentDescription="House Image" />

        <!-- 매칭률 -->
        <TextView
            android:id="@+id/textMatchRate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_margin="8dp"
            android:background="@drawable/bg_feed_tag_chip"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:text="87% Match"
            android:textColor="@android:color/white"
            android:textStyle="bold" />

        <!-- 제목 + 가격 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingHorizontal="12dp"
            android:paddingTop="4dp">

            <TextView
                android:id="@+id/textTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Officetel"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#0A0A0A" />

            <TextView
                android:id="@+id/textPrice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="₩400-500(k)/mo"
                android:textSize="14sp"
                android:textColor="#0A0A0A" />
        </LinearLayout>

        <!-- 상세 정보 -->
        <TextView
            android:id="@+id/textDetails"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="12dp"
            android:paddingTop="2dp"
            android:text="🏙 Seoul, Gangnam-gu\n🔑 Private Room   💰 W2000(k)\n📅 Available from Dec 15, 2024"
            android:textSize="13sp"
            android:textColor="#717182" />

        <!-- 태그 -->
        <LinearLayout
            android:id="@+id/tagContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_margin="12dp"
            android:gravity="center" />

        <!-- 설명 -->
        <TextView
            android:id="@+id/textDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="12dp"
            android:paddingBottom="12dp"
            android:text="Tech enthusiast and gym lover. Work from home mostly. Looking for someone responsible and clean."
            android:textSize="14sp"
            android:textColor="#0A0A0A" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
